<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="70dp"
    android:clipChildren="false">

    <LinearLayout
        android:id="@+id/tooltip_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="-10dp"
        android:background="@drawable/bg_tooltip_white"
        android:elevation="8dp"
        android:paddingHorizontal="6dp"
        android:paddingVertical="2dp"
        android:visibility="gone">
        <TextView
            android:id="@+id/tv_tooltip_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#1e293b"
            android:textSize="9sp"
            android:textStyle="bold" />
    </LinearLayout>

    <View
        android:id="@+id/view_today_highlight"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center"
        android:background="@drawable/circle_white_10"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/img_sleep_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_cruelty_free" />
</FrameLayout>